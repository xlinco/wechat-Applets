function formatRate(v) {
  var lightStar = parseInt(v) / 2;
  var star = [];
  for (var i = 0; i <= 5; i++) {
    if (i <= lightStar) {
      star.push(1);
    } else {
      star.push(0);
    }
  }
  return star;
}

module.exports = {
  formatRate: formatRate
}


